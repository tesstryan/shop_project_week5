<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>


<% @products.each do |product| %>
  

      <%= link_to image_tag(product.image), product_url(product.id) %>

      <br><br>
  
      <%= link_to product.title, product_url(product.id) %> <%= number_to_currency(product.price) %>
      
      <br>

<% end %>




